let config = {};

// TODO change these environments to company specific name when deploying front end app.
// this is app specific such as firebase api info

//   config = require(`./export.json`);
    config = require(`./denver_partial.json`);

export default config;